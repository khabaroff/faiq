# TODO — 2026-05-14 (завтра сделать)

Первый полный прогон A→B→C→D→E завершён. Пайплайн работает, но есть конкретные задачи.

---

## 1. КРИТИЧНО: Починить wiki-ссылки (C → --force)

**Проблема:** Pipeline D нашёл 129 страниц с `needs_cleanup` — у всех одна причина: `../summaries/slug.md` вместо `[[slug]]` в wiki-страницах.

**Причина:** Промпт для C генерирует относительные ссылки на файлы вместо wiki-синтаксиса.

**Что делать:**
1. Открыть `prompts/wiki_update.md` (или аналогичный) — найти шаблон ссылок, заменить `../summaries/` на `[[` + `]]`
2. Запустить `python -m guides.pipelines.c_wiki_update --force`
3. Проверить одну страницу вручную

---

## 2. ВАЖНО: Пересуммировать 17 статей (B --force)

**Проблема:** Pipeline D вернул `needs_resummarize` на 17 саммари — найдены галлюцинации (факты не из исходника).

**Что делать:**
```bash
# Получить список:
cat .state/quality-report.json | python3 -c "
import json,sys
r=json.load(sys.stdin)
for x in r['results']:
    if x.get('verdict')=='needs_resummarize':
        print(x['slug'])
"

# Пересуммировать каждый:
python -m guides.pipelines.b_summarize --slug SLUG --force
# Или батчем если мало:
python -m guides.pipelines.b_summarize --force --batch 20
```

После — запустить D снова чтобы проверить.

---

## 3. VPS DEPLOY — не готов

**Статус:** Deploy не настроен. GitHub Actions не подключён к VPS.

**Файлы:**
- `_human/vps-deploy.md` — план деплоя, прочитай перед началом
- CI существует, но workflow нет (или не вызывает деплой)

**Что нужно:**
1. Настроить SSH-ключ на VPS
2. Создать GitHub Actions workflow `deploy.yml` — push to main → ssh + quartz build
3. Проверить что Quartz поднят на VPS (см. `quartz-setup.md`)

---

## 4. РЕШЕНИЕ НУЖНО: personal vs product (P0.1)

**Файл:** `_human/decisions-pending.md` — раздел P0.1

Пайплайн сейчас работает как личный инструмент. Нужно решить: остаётся личным или становится продуктом? Это влияет на scope всего следующего спринта. Без решения нельзя планировать фичи.

---

## 5. Баги кода (не блокируют, но починить)

### 5.1 `json_extract.py` — escaped quote edge case

**Файл:** `src/guides/json_extract.py:60`

LLM иногда экранирует последнюю кавычку перед `}`:
```
{"text": "...labeled \"Opus 4.7.\"}"   ← здесь } попадает внутрь строки
```
Фикс: добавить `try: json.loads(text.strip())` как первый шаг в `extract_json` перед bracket-counting.

### 5.2 OCR — счётчик ошибок

В pipeline A нет счётчика OCR-failures. Добавить логирование: "Processed X items, Y OCR failures" + slug + image URL при каждом failure.

---

## 6. Pipeline статистика прогона

| Pipeline | Время | Результат |
|----------|-------|-----------|
| A (ingest) | 14m 33s | 28 файлов обработано |
| B (summarize) | ~31s/файл | 31/31 ✓ (после отключения jailbreak-filter) |
| C (wiki update) | 4m 33s | 250 страниц |
| D (quality check) | 6m 25s | 5 ok / 12 minor_fix / 17 needs_resummarize / 129 needs_cleanup |
| E (SEO) | 45s | 31/31 ✓ |

**Важно:** Azure jailbreak-filter отключён вручную в Azure AI Foundry. Без этого B падает на 100% AI-статей.

---

## 7. Open decisions (остальные)

Полный список в `_human/decisions-pending.md`. Приоритет:

- **P0.1** — personal vs product (блокирует планирование)
- **P0.2** — .env policy (сейчас `AZURE_OPENAI_API_KEY` в .env, не в secrets)
- **P1.1** — healthcheck при старте (endpoint format validation)

---

*Записано 2026-05-14 после первого полного прогона пайплайна.*
